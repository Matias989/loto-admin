# Railway - Albion Guild Bot
# Los volúmenes se configuran en el dashboard (no vía archivo)

[build]
builder = "nixpacks"

[deploy]
startCommand = "npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
